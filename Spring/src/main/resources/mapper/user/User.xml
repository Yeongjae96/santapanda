<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.santapanda.user.mapper.UserMapper">

	<select id="login" resultType="kr.co.santapanda.user.model.UserVO">
		SELECT user_id
			 , password
			 , name
			 , email
			 , use_yn
		  FROM com_user
	     WHERE user_id = #{userId}
	       AND password = #{encryptPassword}
	</select>
	
	<insert id="createUser" parameterType="kr.co.santapanda.user.model.UserVO">
		INSERT INTO com_user
				  ( user_id
				  , password
				  , name
				  , email
				  , use_yn )
		   VALUES ( #{userId}
				  , #{password}
				  , #{name}
				  , #{email}
				  , #{useYn} )
	</insert>
	
	<select id="getUserInfo" parameterType="kr.co.santapanda.user.model.UserVO">
		SELECT user_id
			 , password
			 , name
			 , email
			 , use_yn
		  FROM com_user
	</select>
</mapper>